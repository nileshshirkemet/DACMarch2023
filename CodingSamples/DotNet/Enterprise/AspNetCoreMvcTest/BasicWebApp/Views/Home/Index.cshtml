@model Tourism.Site
<html>
    <head>
        <title>BasicWebApp</title>
    </head>
    <body>
        <h1>Visitors of @Model.Title</h1>
        <table border="1">
            <tr>
                <th>Visitor Name</th>
                <th>Visit Count</th>
                <th>Last Visit</th>
            </tr>
            @foreach (var visitor in Model.Visitors)
            {
                <tr>
                    <td>@visitor.Id</td>
                    <td>@visitor.VisitCount</td>
                    <td>@visitor.LastVisit</td>
                </tr>
            }
        </table>
        <p>
            <form method="post" action="/Home/Register">
                <b>Name:</b>
                <input type="text" name="guest" required="required"/>
                <input type="submit" value="Register"/>
            </form>
        </p>
    </body>
</html>